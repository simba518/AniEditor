SET(APP_NAME BasisGenerator)

AUX_SOURCE_DIRECTORY(. BasisGenerator_src)

ADD_EXECUTABLE(${APP_NAME} 
  ${BasisGenerator_src}
  )

TARGET_LINK_LIBRARIES(${APP_NAME}
  
  ${Boost_LIBRARIES}
  ${ARPACK_LIBRARIES}
  ${SUPERLU2.0_LIBRARIES}
  ${LAPACK_LIBRARIES}
  ${SUITESPARSE_LIBRARIES}
  )